import Head from "next/head";
import Image from "next/image";
import data from "../../data.json"
import { useState } from "react";

const colorMap = {
    "Reaction-bg": "bg-red-50",
    "Memory-bg": "bg-yellow-50",
    "Verbal-bg": "bg-green-50",
    "Visual-bg": "bg-blue-50",
    "Reaction-text": "text-red-400",
    "Memory-text": "text-yellow-400",
    "Verbal-text": "text-green-400",
    "Visual-text": "text-blue-400",
} as const

export default function Index() {
    const [items] = useState(data);
    
    
    return (
        <>
            <Head>
                <title>Create Next App</title>
                <meta name="description" content="Generated by create next app" />
                <meta name="viewport" content="width=device-width, initial-scale=1" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <main className="bg-white">
                <article>
                    <div className="p-4 bg-gradient-to-t from-blue-700 to-violet-500">
                        <h3>Your result</h3>
                        <p>
                            <span>76</span> of 100
                        </p>
                        <h2>Great</h2>
                        <p>Your scored higher than 65% of the people who have takenthese tests</p>
                    </div>
                    <div className="text-black p-4">
                        <h2>Summary</h2>
                        <div>
                            {items.map((item) => (
                                <div key={item.category}
                                    className={`${colorMap[item.category+"-bg" as keyof typeof colorMap]} flex`}>

                                    <Image
                                        alt={item.category}
                                        src={item.icon}
                                        width={22}
                                        height={22}
                                        className="flex-none">
                                    </Image>
                                    <h3 className={`flex-1 ${colorMap[item.category+"-text" as keyof typeof colorMap]} font-bold`}>{item.category}</h3>
                                    <p>{item.score} <span className="text-gray-500">/ 100</span></p>
                                </div>
                            ))}
                        </div>
                        <button className="rounded-3xl h-14 w-24 bg-blue-900 text-white">Continue</button>
                    </div>
                </article>
            </main>
        </>
    )
}

export async function getServerSideProps() {
    return {
        props: {}, // will be passed to the page component as props
    }
}